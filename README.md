# highlightjs-pyret

A [highlight.js](https://highlightjs.org) plugin for the [Pyret](https://pyret.org/) programming language.

## Usage

```javascript
import hljs from 'highlight.js';
import pyret from 'highlightjs-pyret';

hljs.registerLanguage('pyret', pyret);
hljs.highlightAll();
```
